import OrderForm from "@/components/order/OrderForm.component";

/** `/commandes/nouvelle` : la page n'assemble rien, le formulaire fait le travail. */
const OrderFormPage = () => <OrderForm />;

export default OrderFormPage;
